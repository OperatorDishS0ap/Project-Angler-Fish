Install Python Packages:
```
pip install opencv-python pygame paramiko numpy pillow
```

Run the project natively on the PC and  not in a virtual environment. Run anglerfish.py to start the program.

